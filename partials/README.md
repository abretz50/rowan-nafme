# Rowan NAfME — Starter Website
Run with a local server so partials load (e.g., `python -m http.server 8000`). Deploy from GitHub to Netlify; enable Netlify Identity. Wire Neon via Netlify Functions later.
